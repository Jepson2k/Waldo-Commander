# Session handoff — 2026-09-10

The user ended the debugging session and requested cleanup, commits and pushes as necessary. Physical homing, simulator calibration and movement quality are **not solved**. No further robot work is authorized by this checkpoint.

## Commander

This branch preserves the explicit `--dev-mcp-autopilot` / `WALDO_DEV_MCP_AUTOPILOT` option requested for local development. It skips per-session hardware consent, binds MCP to loopback, and preserves ownership and per-action control-mode gates.

Validation at cleanup: the three focused MCP tests passed (normal session consent, development sessions/browser ownership, and denial cooldown); Ruff passed. Pytest emitted nanobind reference-leak diagnostics at interpreter shutdown despite the three passing tests. The full Commander `ty` hook also reports one error in unchanged `services/simulation_scenarios.py:120`: the installed `DryRunRobotClient` type lacks `skill_capabilities`. That hook was skipped for this WIP checkpoint after recording the failure; no functional fixes were attempted after the user ended the session.

## Native runtime

The native source checkout is now `par6/`, on the matching `wip/homing-simulator-20260910` branch of `Jepson2k/par6`. Native source checkpoint **f3f50ff** is preserved there and pushed. Its `docs/development/2026-09-10-handoff.md` records changes, tests, deployment differences and unresolved failures. The old `bench/par6-runtime` linked worktree was removed after preserving its changes.

That checkpoint is WIP: the independent physical-load calibration test fails, the J1 gain candidate has only simulator evidence, and the local Python type-check hook fails against the mixed dependency environment. The failed type-check hook was skipped explicitly for preservation. This is not a verified release or a completed calibration routine.

## Hardware and local evidence

- The last run used the visible `load_aware_homing.py` after removing the agent-added 146-degree J2 cutoff at the user's request.
- The final recording, formerly `bench/2026-09-10/motion-capture-20260910T115700Z`, showed 31.959 seconds of active homing before native error 58 (`Joint 2 reported CanLost`, zero-based joint identifier) disabled the controller. Complete homing was not achieved.
- Cleanup stopped the program, cleared motion, switched to simulator mode and verified `connected=false, simulator_active=true` before terminating Commander and its managed native runtime. The passive recorder also exited. Nothing remains running from the session.
- Calibration notes and UART transactions, compact motion analyses, the startup trial configuration, and the exact final program source are retained locally in `.git/cleanup-archives/2026-09-10/hardware-evidence/`; [the evidence index](2026-09-10/README.md) describes them. The source is stored as `last-homing-program.py.txt` and retains the removal of the displacement cutoff.
- At the user's request, `bench/`, this project's `.local/`, and `/home/rbt/waldo-migration` were removed. Large motion captures, plots and build caches were deleted; the original capture paths in historical notes no longer exist.

No executable was rebuilt or installed during cleanup. The experimental bench executable was deleted with its build directory; its trial configuration was retained as evidence. The installed wheel still contains its packaged runtime. Read the native handoff before choosing any future runtime or gains; the source checkpoint and the last executed binary were different.

## Transfer cleanup

The installed `waldoctl` and `parol6` editable dependencies now resolve to `waldoctl/` and `PAROL6-python-API/` in this repository, at the same commits as their former transfer checkouts (`0b88b501` and `ca62e2fb`). Their prior local branches remain available.

Transferred Git refs and worktree heads were preserved under local `refs/archive/transfer-20260910/` and `refs/archive/source-pi-20260910/` namespaces in the corresponding repositories. Manifests are in the documentation index directory. Older unrelated uncommitted patches, orphan worktree source and diagnostic scripts are retained locally in `.git/cleanup-archives/2026-09-10/`; they were not published as Commander changes. The home-wide `/home/rbt/.local` directory was left intact.
